import Vue from 'vue'
import Vuex from 'vuex'
import menu from '@/data/menu'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
    menu
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
